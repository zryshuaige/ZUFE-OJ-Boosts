#include<bits/stdc++.h>
using namespace std;

int main()
{
	stack<char>st;
	string s;
	cin>>s;
	for(int i=0;i<s.size();i++)
	{
		if(s[i]=='('||s[i]=='[')
		{
			st.push(s[i]);
		}
		else if(s[i]==')'||s[i]==']')
		{
			if(s[i]==')'&&st.top()!='('||s[i]==']'&&st.top()!='[')
			{
				cout<<"Wrong";
				return 0;
			}	
			st.pop();
		}
	}
	if(st.empty())	cout<<"OK";
	else cout<<"Wrong";	
	return 0;
}
